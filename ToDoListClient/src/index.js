// אחרי (React 18)
import React from 'react';
import ReactDOM from 'react-dom/client'; // שים לב: זה שינוי בייבוא
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
